export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1" style={{ color: 'var(--wd-dark)' }}>
        Ustawienia
      </h1>
      <p style={{ color: 'var(--muted-foreground)' }} className="text-sm">
        Ustawienia systemu — dostępne wkrótce.
      </p>
    </div>
  )
}
